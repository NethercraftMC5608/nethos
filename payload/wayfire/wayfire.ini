# NETHOS under Wayfire.
#
# Wayfire rather than sway, for three things sway structurally cannot do:
#
#   * windows float by default and stack, the way a desktop is expected to
#     behave, instead of being tiled into a layout you did not ask for
#   * real decorations, so a window has minimise, maximise and close where
#     everyone's hand already reaches for them
#   * blur, done in the compositor -- which is what makes the glass real. In
#     CSS, backdrop-filter is per-frame work over the whole surface and it is
#     what made the shell unusable on software rendering. Here it is the
#     compositor's job and costs the shell nothing.
#
# It also snaps on drag, which sway has no concept of.

[core]
plugins = \
  autostart \
  ipc \
  ipc-rules \
  stipc \
  alpha \
  animate \
  blur \
  command \
  firedecor \
  expo \
  fast-switcher \
  grid \
  move \
  place \
  resize \
  vswitch \
  window-rules \
  wm-actions

# One workspace. Rule 9: the desktop is a desk, not a filing system, and
# workspace grids are a power feature that costs every user a mental model.
vwidth = 1
vheight = 1

close_top_view = <super> KEY_Q | <super> KEY_W

# What the screen is before anything has painted on it.
#
# Wayfire's default is black, and the desktop surface does not exist until
# nethos-view starts -- about four seconds after the compositor on this
# machine. Four seconds of black after a boot reads as a machine that has
# failed, which is why "black screen" was on the list. This is the pale tone
# the default wallpaper resolves to, so the first thing on screen is already
# the right colour and the wallpaper arrives as a detail rather than as a
# change.
background_color = \#161B22FF

# ---------------------------------------------------------------- glass --
[blur]
# kawase is the cheap one and the difference from gaussian is not visible at
# these radii. iterations trade cost against smoothness; 2 is the sweet spot.
method = kawase
# 3 iterations rather than 2: with the denser surface fills the shell now
# uses, a tighter blur showed the wallpaper's shapes through the glass as
# recognisable objects, which is the thing that stops it reading as frost.
kawase_iterations = 3
kawase_offset = 4.0
kawase_degrade = 1
# Saturation lifted very slightly, the way frosted glass concentrates colour.
saturation = 1.15
# Layer surfaces -- the panel, the dock, the launcher -- blur too, which is the
# whole point: they are what should look like glass.
blur_by_default = type is toplevel | type is layer

[alpha]
# Windows are slightly transparent so the blur behind them reads as depth
# rather than as a flat frosted pane. Small: past about 8% it stops looking
# like glass and starts looking like a bug.
min_value = 0.5
modifier = <super> KEY_A

# ------------------------------------------------------------ windows --
# firedecor, not Wayfire's own `decoration`.
#
# The built-in decorator cannot do what NETHOS needs and it is not close: its
# entire option set is font, title_height, border_size, button_order,
# active_color, inactive_color and ignore_views. No corner radius -- the
# corner_radius that used to sit in this file was read by nothing -- and no
# way to move the buttons off the right.
#
# firedecor is packaged (reform-firedecor) and has both: a corner radius, and
# a layout string that puts the buttons where we want them. This makes a
# foreign window -- Chromium, Thunar, the terminal -- wear the same rounded
# frame and the same three lights as a NETHOS window, which is the difference
# between a desktop and a collection of applications.
# Never hot-swapped. Changing the decoration plugin in a running Wayfire --
# editing this file while it is live -- crashed the compositor outright and
# took the desktop with it, stack trace and all. The same config starts
# perfectly from cold. Change it, then restart the session
# (`systemctl restart getty@tty1`), or test it nested first:
#     wayfire -c /path/to/test.ini
# which opens a whole second compositor in a window and cannot hurt anything.
[firedecor]
# Regions are separated by `|`: left | centre | right | corners. The default
# is `a | icon P4 title | minimize p maximize p close p | Atrtl -`, which is
# title-left, buttons-right. Putting the buttons in the first region is what
# moves them to the macOS position; `p` is one padding unit, `P10` is ten.
#
# Verified in a nested Wayfire before going anywhere near the real session --
# see the note on hot-reloading below.
layout = P10 close p minimize p maximize | title | P10 | Atrtl -

# docs/DESIGN.md 31: larger than feels right. Matches the 14px the NETHOS
# window chrome uses, so the two are indistinguishable at a glance.
corner_radius = 14
round_on = all

# 38px bar, 1px border. A 0 border leaves nothing to grab for a resize.
border_size = 38 1
button_size = 13
button_style = simple

# The same traffic lights the NETHOS chrome draws, including the brighter
# hover state -- firedecor gives us a hover colour per button, which is how
# the two stay in step.
normal_close  = \#EC6A5EFF
hovered_close = \#FF7C70FF
normal_min    = \#F4BF4FFF
hovered_min   = \#FFD166FF
normal_max    = \#61C554FF
hovered_max   = \#75D967FF
# Grey when the window is not focused, for the same reason the NETHOS chrome
# greys: the colour says which window is listening.
inactive_buttons = true

active_border   = \#161B22E0
inactive_border = \#161B22E0
active_title    = \#F1F4F8FF
inactive_title  = \#BDC6D1FF
active_accent   = \#161B22E0
inactive_accent = \#161B22E0
outline_size = 0

[move]
# The snapping sway does not have. Drag a window into an edge and it takes
# that half; into a corner and it takes that quarter. 20px of slop, because a
# threshold you have to aim for is worse than none.
activate = <super> BTN_LEFT
enable_snap = true
enable_snap_off = true
snap_threshold = 20
snap_off_threshold = 20
join_views = true

[resize]
activate = <super> BTN_RIGHT

[grid]
# The keyboard half of the same idea, matching the keys Windows uses.
slot_l = <super> KEY_LEFT
slot_r = <super> KEY_RIGHT
slot_t = <super> KEY_UP
slot_b = <super> KEY_DOWN
slot_tl = <super> <ctrl> KEY_LEFT
slot_tr = <super> <ctrl> KEY_RIGHT
slot_bl = <super> <shift> <ctrl> KEY_LEFT
slot_br = <super> <shift> <ctrl> KEY_RIGHT
# The animation when a window snaps. Short, eased, and over. Rule 24: motion
# you can describe afterwards took too long.
type = crossfade
duration = 200

[wm-actions]
toggle_maximize = <super> KEY_F
minimize = <super> KEY_H

# ------------------------------------------------------------- motion --
[animate]
# 200ms in, 150ms out, no bounce and no overshoot. Rule 24.
open_animation = zoom
close_animation = zoom
duration = 200
startup_duration = 300
fade_duration = 150

[expo]
toggle = <super> KEY_E
duration = 250

[fast-switcher]
activate = <alt> KEY_TAB

# -------------------------------------------------------------- shell --
[autostart]
# Deliberately no wf-panel and no wf-background: NETHOS draws its own panel,
# dock and desktop. Starting Wayfire's as well gives two top bars.
# nethosd first: the surfaces load their content from it, and a shell that
# starts before its API paints an empty panel.
nethosd = systemctl --user start nethosd
shell = sh -c 'sleep 1; nethos-session'
# No wallpaper daemon: desktop.html draws the background, and two things
# drawing a background is how the dock ended up eating clicks under sway.
portal = /usr/libexec/xdg-desktop-portal-wlr

[command]
binding_terminal = <super> KEY_RETURN
command_terminal = foot
binding_launcher = <super> KEY_D
command_launcher = nethos-menu-toggle
binding_launcher2 = <super> KEY_SPACE
command_launcher2 = nethos-menu-toggle
binding_reload = <super> <shift> KEY_R
command_reload = nethos-reload

[window-rules]
# The shell's own surfaces are layer-shell and never windows, so nothing here
# is about them. This is for applications that open badly.
rule_1 = on created if app_id is "foot" then start_on_output ""
